import Link from 'next/link';

const Header = () => {
    return (
        <header>
            <nav>
                <Link href="/">Startsida</Link>
            </nav>
        </header>
    );
}

export default Header;